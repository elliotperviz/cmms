NEB tutorial

Use atomsk, convert
$ initial.sivac -> POSCAR
and visualise in VESTA - highlight atom to remove

View input file
--> Modify to allow wider structural relaxation?
---> Requires relaxation of end point with vacancy, creating different positions before and after

mpirun -np 4 lmp -partition 4x1 -in in.neb.sivac
mpirun --oversubscribe -np 8 lmp -partition 4x2 -in in.neb.sivac
mpirun --oversubscribe -np 8 lmp -partition 8x1 -in in.neb.sivac
mpirun --oversubscribe -np 16 lmp -partition 8x2 -in in.neb.sivac

neb_final.py -o dump.neb.sivac.final -r dump.neb.sivac.*
neb_final.py -o dump.nebnear.sivac.final -r dump.nebnear.sivac.*
neb_final.py -o dump.all.sivac.final -r dump.all.sivac.*

Visualise trajectory in Ovito
